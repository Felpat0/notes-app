export type Measure = {
    x: number;
    y: number;
    width: number;
    height: number;
    pageX: number;
    pageY: number;
};

export type DropdownOption = {
    label: string;
    // eslint-disable-next-line
    value: any;
};
