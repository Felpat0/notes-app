import { StyleSheet } from "react-native";

export const checklistElementStyles = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
    },
    input: {
        flex: 1,
        marginLeft: 2,
        height: 37,
    },
});
